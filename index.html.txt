<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  />
  <title>Mauna Loa CO₂ Today</title>

  <!-- App manifest for installable “app” behaviour -->
  <link rel="manifest" href="manifest.json" />

  <!-- Simple, clean styling -->
  <style>
    :root {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
      background: #0b1724;
      color: #f5f7fb;
      --accent: #4fd1c5;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(circle at top, #1b3248, #050b11);
    }

    .card {
      width: min(420px, 90vw);
      border-radius: 18px;
      padding: 24px 20px 20px;
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.4);
      background: rgba(7, 16, 26, 0.92);
      backdrop-filter: blur(8px);
      border: 1px solid rgba(255, 255, 255, 0.06);
    }

    .title {
      font-size: 1.1rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: #9fb3c8;
      margin-bottom: 6px;
    }

    .subtitle {
      font-size: 0.9rem;
      color: #7f8fa3;
      margin-bottom: 22px;
    }

    .value-row {
      display: flex;
      align-items: baseline;
      gap: 8px;
      margin-bottom: 6px;
    }

    .co2-value {
      font-size: 3.2rem;
      font-weight: 600;
      color: var(--accent);
      line-height: 1;
    }

    .unit {
      font-size: 1rem;
      color: #a9bbd4;
    }

    .meta {
      font-size: 0.8rem;
      color: #7f8fa3;
      margin-top: 6px;
    }

    .meta strong {
      color: #cbd5f5;
      font-weight: 500;
    }

    .status {
      font-size: 0.8rem;
      margin-top: 16px;
      color: #a0aec0;
    }

    .status span {
      color: var(--accent);
    }

    .error {
      color: #feb2b2;
    }

    .footer {
      font-size: 0.75rem;
      color: #64748b;
      margin-top: 18px;
      border-top: 1px solid rgba(148, 163, 184, 0.2);
      padding-top: 8px;
      display: flex;
      justify-content: space-between;
      gap: 8px;
      flex-wrap: wrap;
    }
  </style>
</head>
<body>
  <main class="card">
    <div class="title">Atmospheric CO₂</div>
    <div class="subtitle">Mauna Loa Observatory · Daily mean</div>

    <div class="value-row">
      <div id="co2-value" class="co2-value">—</div>
      <div class="unit">ppm</div>
    </div>

    <div id="date-label" class="meta">
      Latest date: <strong>—</strong>
    </div>

    <div id="status" class="status">
      Fetching latest value<span>…</span>
    </div>

    <div class="footer">
      <div>Source: NOAA GML (Mauna Loa CO₂)</div>
      <div>Updates when you open the app.</div>
    </div>
  </main>

  <script src="app.js"></script>
<script>
  if ("serviceWorker" in navigator) {
    window.addEventListener("load", () => {
      navigator.serviceWorker
        .register("./service-worker.js")
        .catch((err) => console.error("SW registration failed:", err));
    });
  }
</script>
</body>
</html>
